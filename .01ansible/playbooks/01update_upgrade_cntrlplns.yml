---
- name: Update and Upgrade Raspberry Pi Servers
  hosts: all
  become: yes
  tasks:
    - name: Update apt repo and cache on all Raspberry Pis
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Upgrade all packages to the latest version
      apt:
        upgrade: 'yes'
